<main class="main">
  <div class="inner">
    
    <% if user_signed_in?  %>
      <div class="greeting">
        
        <%= link_to "ようこそプロトスペースへ、#{current_user.name}さん", root_path, class: :greeting__link %>
      </div>
    
      <div class="card__wrapper">
        <%# "投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する" %>
      </div>
    
    <% else %>

    <% end %>

  </div>
</main>